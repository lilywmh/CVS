# Notebooks

Exploratory notebooks can live here, preferably under `exploratory/`.

For reproducibility, the canonical workflow is maintained in `scripts/` rather
than notebooks. If a notebook becomes part of the replication path, convert the
core logic into a script and reference it from `scripts/README.md`.
