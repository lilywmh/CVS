# Example local path configuration for a private replication checkout.
# Do not commit real data folders. Copy this file to a private notes/config
# location if needed, or set the matching environment variables below.

transcripts_root: data/raw/all_srt
whisperx_outputs: data/interim/whisperx_outputs
wav_dir: data/interim/wav_16k
derived_data: data/derived
private_data: data/private
analysis_outputs: outputs
figures: figures

transcription_log: data/derived/Discussion Transcription Log - Sheet1.csv
outcomes: data/derived/outcomes.csv
participant_master: data/private/master_sheet_one_row_per_participant.csv

# Equivalent environment variables used by selected scripts:
# CVS_SRT_ROOT=data/raw/all_srt
# CVS_WHISPERX_OUTPUTS=data/interim/whisperx_outputs
# CVS_WAV_DIR=data/interim/wav_16k
# CVS_DATA=data/derived
# CVS_ANALYSIS_OUTPUTS=outputs
# CVS_FIGURES=figures
